# Core CrewAI framework with all provider support
crewai>=0.80.0
crewai-tools>=0.17.0

# LLM Provider support
# Anthropic (Claude) support
anthropic>=0.40.0

# Google Gemini support
google-generativeai>=0.8.0

# Note: Ollama support is built-in via litellm (bundled with crewai)
# litellm>=1.44.22 is included as a crewai dependency

# Configuration and environment
PyYAML>=6.0.1
python-dotenv>=1.0.1

# UI and utilities
streamlit>=1.40.0
rich>=13.7.0

# Optional: For yWriter7 XML handling improvements
lxml>=5.0.0

# Vector database for RAG and semantic search
chromadb>=0.5.0

# Embedding models for ChromaDB
sentence-transformers>=2.7.0
